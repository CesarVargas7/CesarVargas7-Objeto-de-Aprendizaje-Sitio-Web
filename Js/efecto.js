const imagen = document.getElementById('imgR');
    let rotate360 = [
      { transform: 'rotate(360deg)' }
    ];

    let slowInfinite = {
      duration: 4500,
      iterations: Infinity
    }
    imagen.animate( rotate360, slowInfinite );
    
const imagen1 = document.getElementById('imgR1');
    let rotate3601 = [
      { transform: 'rotate(360deg)' }
    ];

    let slowInfinite1 = {
      duration: 4500,
      iterations: Infinity
    }
    imagen1.animate( rotate3601, slowInfinite1 );
    
const imagen2 = document.getElementById('imgR2');
    let rotate3602 = [
      { transform: 'rotate(360deg)' }
    ];

    let slowInfinite2 = {
      duration: 4500,
      iterations: Infinity
    }
    imagen2.animate( rotate3602, slowInfinite2 );